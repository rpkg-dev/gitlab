list(
  rd_family_title = list(commits  = "Other functions to manage commits on GitLab:",
                         common   = "Other common functions around GitLab's v4 RESTful API:",
                         dirs     = "Other functions to manage directories on GitLab:",
                         files    = "Other functions to manage files on GitLab:",
                         projects = "Other functions to manage GitLab projects:")
)
